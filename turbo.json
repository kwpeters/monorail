{
  "$schema": "https://v2.turborepo.dev/schema.json",
  "ui": "tui",
  "globalPassThroughEnv": [
    "USERPROFILE",
    "NODE_ENV",
    "PORT"
  ],
  "tasks": {
    "build": {
      "description": "Build all packages and applications",
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*", "!README.md", "!**/*.test.mts"],
      "outputs": ["dist"]
    },
    "lint": {
      "description": "Lint all packages and applications",
        // Linting does not depend on anything from any other project.  So run
        // with maximum parallelism.
      "dependsOn": []
    },
    "dev": {
      "description": "Start development servers",
      "cache": false,
      "persistent": true
    },
    "test": {
      "description": "Run unit tests",
        // Running unit tests does not depend on anything from any other
        // project.  So run with maximum parallelism.
        "dependsOn": [],
        "outputs": []
    },
    "type-check": {
      "description": "compile all packages and applications, including unit tests",
        "dependsOn": [
            "^type-check"
        ],
        "inputs": [
            "$TURBO_DEFAULT$",
            ".env*"
        ],
        "outputs": [
        ]
    },
    "depcheck": {
      "description": "Check for unused and missing dependencies",
        // Running depcheck does not depend on anything from any other
        // project.  So run with maximum parallelism.
        "dependsOn": [],
        "outputs": []
    }
  }
}
