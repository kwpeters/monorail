// Can be either a human player's hand or a bot's hand.
export class PlayerHand {

    // The card drawn at the beginning of the player's turn, if they are the
    // active player.
    // drawCard: Option<MilleBornesCard>

    // The cards in the player's hand.
    // handCards: Array<MilleBornesCard>
}
